{
  "name": "test-project",
  "version": "1.0.0",
  "description": "Test project for development of golang/vue.js web applications with docker containers",
  "author": "Dr. med. Ramon Saccilotto, Department of Clinical Research, University Hospital Basel",
  "scripts": {
    "dev": "webpack-dev-server --content-base ./public/ --inline --hot --quiet --host 0.0.0.0 --port 8080",
    "build": "cross-env NODE_ENV=production webpack --progress --hide-modules --config webpack.deploy.config.js"
  },
  "dependencies": {
    "vue": "^1.0.15",
    "vue-resource": "^0.7.0",
    "vue-router": "^0.7.10"
  }
}
