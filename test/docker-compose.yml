services:
  app:
    container_name: hot-reload-go.1.25.3
    image: dkfbasel/hot-reload-go:1.25.3
    volumes:
      - ./src:/app
    environment:
      - TIMEOUT=800ms
      - IGNORE=*.md,ignorethis,*.log
